#include <iostream>
#include <string>

using namespace std;

int main(int argc, const char * argv[]) {
    string a,b;
    int ans[10001]={0,};
    
    cin >> a >> b;
    if(a.size()>=b.size()){
        
    }
    
    return 0;
}
